const categorieslistEl = document.querySelectorAll(`.item`);
console.log(`Number of categories: ${categorieslistEl.length}`);

const itemListEl = document.querySelectorAll(`h2`);
console.log(itemListEl.textContent);



